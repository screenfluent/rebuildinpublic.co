{
  "name": "rebuildinpublic",
  "scripts": {
    "build": "NODE_ENV=production tailwindcss -i style.css -o output.css --minify",
    "watch": "tailwindcss -i style.css -o output.css --watch"
  },
  "devDependencies": {

  }
}
