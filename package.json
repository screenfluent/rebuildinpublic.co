{
  "name": "rebuildinpublic",
  "scripts": {
    "build": "NODE_ENV=production tailwindcss -i style.css -o build.css --minify",
    "watch": "tailwindcss -i style.css -o build.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.1"
  }
}
